{
  "name": "comfyui-sampler-info-dev",
  "private": true,
  "type": "module",
  "description": "Dev-only test harness for the comfyui-sampler-info pack. Nothing here ships to users — the runtime is the single-file ES module in web/js/. See ADR-0003 and ADR-0009.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.1.7"
  }
}
